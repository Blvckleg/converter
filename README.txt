@author Matteo Juen
//HOW TO USE THE YOUTUBE CONVERTER
*/
/open cmd and type the command "python"
/install python
/go into the right directory where you save the python file from the package with "cd exampledirectory"
/install pytube with the cmd command "pip install pytube"
/open the python file with an editor, developing environment or by changing it to a .txt file
/add the url from the youtube video you'd like to download in the variable "url"
/save the file (and change back to python file if you made it a txt) and run the file with the cmd
/cmd command to run file: "python converter.py"
/cmd should tell you when the program is running and finished
/enjoy
/*